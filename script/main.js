function main() {
    this.game = new Game(['id1', 'id2', 'id3']);

    var IF = new Interface("interface", this.game);

/*
    deck = new Deck();

    players = [
        new Player(),
        new Player(),
        new Player()
    ];

    player = players[0];



    deck.generate();
    deck.shuffle();

    deck.distribute(players, 3);
*/
}
